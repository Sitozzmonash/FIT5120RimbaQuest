# RimbaQuest — Iteration 1

Discover & Collect MVP. Start locally with `docker compose up --build`, then open http://localhost:3000. The demo uses child profile `1` from the supplied SQLite seed schema.

Environment variables are server-side only. Copy `backend/.env.example` to `.env` only when configuring model providers; Iteration 1 does not activate vision or chat UI.
