import { StyleSheet } from 'react-native';

export const collectionStyles = StyleSheet.create({
  collectionVineBottomLeft: { position: 'absolute', left: -15, bottom: -12, width: 90, height: 90, zIndex: 2 },
  collectionTigerTopRight: { position: 'absolute', right: -14, top: -110, width: 200, height: 200, zIndex: 2 },
  collectionVineBottomRight: { position: 'absolute', right: -15, bottom: -12, width: 90, height: 90, zIndex: 2 },
});
